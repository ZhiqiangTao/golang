float默认是64位， 不同类型不能互相赋值， C#是可以的

字符，1位的是byte（uint8） 3位的是rune（int32）

数组的长度是数组类型的一部分

slice切片【左闭右开）

切片的容量cap，是底层数组的容量(切片指向的第一个元素到原始最后的容量)；len是元素的数量

定义切片，底层也是先创建个数组，用切片包装一层

defer 压栈登记，值类型传递的是copy；医用类型传递的是引用